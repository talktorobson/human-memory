openapi: 3.0.3
info:
  title: Memory Gateway API (RAG-only)
  version: 1.0.0
servers:
  - url: http://localhost:8080
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: ok
